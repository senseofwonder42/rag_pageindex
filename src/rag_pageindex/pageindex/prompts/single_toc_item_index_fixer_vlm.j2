You are given a section title and {{ num_images }} page image(s) from a document.
The images are provided in order: the first image is page {{ start_page }}, the second is page {{ start_page + 1 }}, and so on.

Your task is to find the page where this section title first appears as a heading.

Section Title:
{{ section_title }}

Reply with the page number and your confidence in this exact JSON format:
{
    "physical_index": "<physical_index_X>",
    "confidence": "high" | "low"
}
where X is the 1-based page number from the document. If the title does not appear in any of these pages, return:
{
    "physical_index": null,
    "confidence": "low"
}

Set "confidence" to "high" only when the heading is clearly visible at the top of exactly one page with no plausible alternative match. Set "confidence" to "low" if there is any ambiguity.

Directly return the JSON. Do not output anything else.
